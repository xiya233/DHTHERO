services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dhtsearch
    ports:
      - "5432:5432"
    volumes:
      - pgdata_dht:/var/lib/postgresql/data
volumes:
  pgdata_dht:
